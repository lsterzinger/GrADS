version: '2.3'
services:
  test:
    image: grads-arm
    command: ["grads", "-lb"]
    build: .
    environment:
      DISPLAY: $DISPLAY
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ~/.Xauthority:/root/.Xauthority
    network_mode: "host"